server:
  port: 8090

spring:
  application:
    name: msvc-users
  datasource:
    url: jdbc:mysql://yamabiko.proxy.rlwy.net:19992/railway
    username: root
    password: LxizxLpXgHkGpHdXZxinQNagbrfrFJeD
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop   # o validate / create-drop según tu necesidad
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  config:
    import: optional:configserver:http://localhost:888

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
